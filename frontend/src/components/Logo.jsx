const Logo = ({ className = "" }) => {
  return (
    <div
      className={`font-extrabold tracking-tight text-purple-500 ${className}`}
    >
      SnapCart
    </div>
  );
};

export default Logo;
